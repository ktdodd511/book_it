<p><%= review.rating %></p>
<p><%= review.comment %></p>

<% if current_user %>

  <% if review.user_id == current_user.id %>

    <%= link_to "Edit Review", edit_book_review_path(review.book, review) %>
    <%= link_to "Delete Review", book_review_path(review.book, review), method: :delete, data: { confirm: "Are you sure you want to delete this review?" } %>


    <% end %>

<% end %>
